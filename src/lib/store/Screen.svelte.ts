export class Screen {
	width = $state(0);
	height = $state(0);
	gameAreaWidth = $state(0);
	gameAreaHeight = $state(0);
	gameXOffset = $state(0);
	gameYOffset = $state(0);
}

export const screen = new Screen();
